[install]
minimumReleaseAge = 259200 # Only install package versions published at least 3 days ago
minimumReleaseAgeExcludes = ["@types/bun", "typescript"] # These packages will bypass the 3-day minimum age requirement
