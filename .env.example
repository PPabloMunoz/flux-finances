# ==============================================================================
# FLUX FINANCES ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to '.env' and fill in your specific values.
# IMPORTANT: Never commit your actual '.env' file to version control.

# --- DATABASE CONFIGURATION ---
# The connection string for your SQLite database.
# For local development, a relative path like './flux.db' works fine.
DATABASE_URL=./flux.db

# --- APPLICATION SETTINGS ---
# The public URL where the application is accessible.
# Vite uses the 'VITE_' prefix to expose this variable to the frontend code.
VITE_PUBLIC_URL=http://localhost:3000

# The port the application server will listen on internally.
PORT=3000

# --- AUTHENTICATION (Better-Auth) ---
# A random, secure string used to sign session cookies.
# You can generate a fresh one using: openssl rand -base64 32
BETTER_AUTH_SECRET=Agedgh03hk9bHFFtOJf6W5bOjw2BG3hLmDteqoNEXKU=

# A comma-separated list of allowed origins for authentication requests.
# Login/Signup will fail if the request origin is not explicitly listed here.
# Example: http://localhost:3000,https://your-production-domain.com
TRUSTED_ORIGINS=http://localhost:3000

# --- DOCKER DEPLOYMENT (Optional) ---
# These variables are used by docker-compose.yml to configure the container.

# The port on your host machine that maps to the container's internal PORT.
# This is the port you will enter in your browser (e.g., http://localhost:3000).
HOST_PORT=3000
